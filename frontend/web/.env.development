# OAuth redirects go directly to the API gateway (not through Vite proxy).
VITE_API_PUBLIC_URL=http://127.0.0.1:8000
# Match OAUTH_ENABLED in root .env (false = email/password login).
VITE_OAUTH_ENABLED=false
