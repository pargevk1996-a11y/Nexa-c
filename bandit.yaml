exclude_dirs:
  - tests
  - .venv
  - venv
  - .tools
skips:
  - B101  # assert_used — acceptable in test helpers imported by backend code
  - B104  # binding to 0.0.0.0 — intentional in FastAPI service entry points
